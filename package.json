{
  "name": "telegram-mines-demo-miniapp",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx serve frontend -l 3000",
    "dev": "npx serve frontend -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
